# NEXT_ITERATION_METAPROMPT — GRAPH-V2 RECONSOLIDATION

VERIFY LIVE TRUTH BEFORE EXECUTION.

You are entering `SWITZERLAND_JOB_OS` after `/GRAPH-REFACTOR-V2` reconsolidation session `SES-20260901T150500Z-GRAPHV2-001`.

Do not trust the values below over current durable state.

## Mission

Resume the reconsolidation of the already-existing COS V2 coordination architecture with the later multi-niche Candidate/Application OS. Do not start a second architecture. Do not mutate hotel authority merely to make architecture state look current.

## Bootstrap

1. Read current GitHub `main` SHA and ancestry.
2. Read `GOAL.md`, `AGENTS.md`, `STATE.md`, `ARCHITECTURE.md`, `HANDOFF.md`, `TASKS.md`.
3. Read `docs/state/NEXT.json`, `docs/state/v2/project-state.json`, `docs/state/v2/active-claims.json`, `docs/state/v2/checkpoint.json`, `docs/continuity/CONTEXT_SURVIVAL.json`.
4. Read `docs/architecture/V2_ARCHITECTURE.md`, `docs/architecture/MULTI_NICHE_EMPLOYMENT_OS_V1.md`, Candidate/Email contracts and current W5 state.
5. Read this directory's `GRAPH_REFACTOR_V2_RECONSOLIDATION_2026-09-01.md`, graph snapshot and implementation program.
6. Inspect all current open PRs and issues, especially issue #416 and PR #415.
7. Re-read live Drive operational authority before any operational mutation.

## Known bootstrap observations — reverify

At the prior session bootstrap:
- main = `3f96e510fc22246ec69ca22e8f10cf750c7e97cf`;
- E4 authority = `70307f4aea05f8625a3c9c64947d5791535b9d245ce1c278920394c998d94cc6`;
- hotel authority = 690/690;
- H-0691 unallocated;
- CRM universe incomplete;
- outbound CLOSED;
- NEXT = terminal 658 / reconcile 1403;
- project-state and active-claims projections were stale;
- token-6 claim embedded older 657/1404 preconditions;
- Context Survival was stale by its own contract;
- candidate CV assets were private `QA_PENDING`, not human-approved;
- W5 packet compiler was merged; PR #415 proposed exact AAG target binding.

## Highest priority

`GR2-T001`: resolve/supersede stale coordination claim lineage without rewriting history.

Then:
`GR2-T002`: rebuild active claims/project state/graph/ContextPack/CSP from durable events and current main, add stale-precondition regressions, and run zero-context death drill.

Only after coordination state is coherent:
- compile canonical V2.2 integrating coordination + multi-niche employment core;
- compile non-destructive goal compatibility map;
- classify/close stale PRs;
- reconcile candidate asset public state with private QA state;
- adversarially qualify PR #415.

## Hard locks

- no H-ID allocation/reservation;
- no hotel authority promotion;
- no mutation from stale claim/context state;
- no public PII;
- no candidate asset APPROVED without exact private state and required human approval;
- `PACKET_COMPILED != SEND_AUTHORIZED`;
- outbound remains CLOSED absent independent full authorization.

## Closure

Persist every material discovery as issue/decision/test/graph/handoff. A new agent must be able to recover blockers and next safe action without this prompt or any chat history.
