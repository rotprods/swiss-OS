# NEXT_ITERATION_METAPROMPT — COS V2

> VERIFY LIVE TRUTH BEFORE EXECUTION. This packet accelerates resumption; it is not authority.

Project: `SWITZERLAND_JOB_OS`.

1. Read `GOAL.md`, `STATE.md`, `ARCHITECTURE.md`, `HANDOFF.md`, `TASKS.md`, `LEXICON.md`.
2. Read `docs/state/v2/project-state.json`, `active-claims.json`, `checkpoint.json`, `context-pack.json`, `graph-snapshot.json`.
3. Re-read GitHub `main`; reject the ContextPack if main/projection/authority revisions differ.
4. Inventory open PRs and active claims; do not overwrite concurrent progress.
5. Continue the highest-priority READY task in `docs/state/v2/tasks.json`.
6. Preserve the architecture workstream ceiling: no hotel authority mutation, H-ID allocation/reservation or outbound execution unless a separately authorized domain wave explicitly changes that ceiling through its own authority gates.
7. For every material action emit an event and update projection/ContextPack/handoff before closure.
8. Run the V2 gauntlet: duplicate/replay, stale main, overlapping claims, fencing, security, projection recovery and agent-death drill.
9. Never turn similarity/review evidence into identity authority without the existing domain contracts.
10. Before merge/release, run repository CI and reread live main again.

Immediate successor objective after this V2 branch is green: migrate one real active domain workstream to Session→Claim→Event→ContextPack semantics and empirically measure zero-context resumption and collision handling.
