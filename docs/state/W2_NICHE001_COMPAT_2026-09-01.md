# W2 — NICHE-001 HOTEL COMPATIBILITY — 2026-09-01

Closure state: **SAFE_STOP_CANARY / P0 AUTHORITY DRIFT**

## Bootstrap

- parent main: `f0880d1d403358d664091fca46ff9d4cc70e1a4f`
- authority epoch observed: `HS_ENTITY_EPOCH_2026-08-25_E4`
- authority materialized SHA pointer: `70307f4aea05f8625a3c9c64947d5791535b9d245ce1c278920394c998d94cc6`
- live HOTELS_MASTER / HOTELS_V2 scan on 2026-09-01: exactly 690 H-rows, H-0001..H-0690; H-0691 absent/unallocated
- outbound: remains CLOSED

## Implemented

- `src/swiss_os/hotel_niche_projection.py`
  - deterministic H-ID -> ORG-HOTEL-ID mapping
  - non-authoritative materialization into generic organizations/NICHE-001/locations/bridge
  - idempotent reruns using insert-or-ignore
  - drift-preserving semantics: existing divergence is not overwritten
  - semantic mismatch detector
  - compatibility receipt
- `scripts/w2_niche001_compat.py`
  - source DB is copied to a distinct canary DB
  - overlay and projection run only on the copy
  - source authority DB mutation is forbidden by path check
  - JSON receipt and nonzero exit on mismatch
- `tests/test_hotel_niche_projection.py`
  - deterministic ID
  - 1:1 materialization
  - SQLite integrity/FK
  - same-name/different-city distinctness
  - idempotent rerun
  - adversarial drift fail-closed
  - invalid bridge FK failure

## Live authority blocker

W2 MUST NOT be declared `VERIFIED_EQUIVALENT 690/690` yet because the previously identified H-0580 DB↔Sheet semantic drift remains material.

Live Sheet H-0580 currently represents Casa Alva, Trin with current-snapshot/current-reconciled semantics. The reconstructed E4 DB state previously carried older source/state semantics for the same PK. Therefore PK-set/count equality is insufficient to certify semantic equivalence.

Required next authority repair:

1. re-attest H-0580 against T1/current evidence;
2. choose the correct canonical claim-level values from evidence, not convenience;
3. perform one constrained synchronized DB + Sheets + graph/observability transaction;
4. regenerate authority manifest/materialized SHA if authority changes;
5. run W2 source->canary projection against the repaired authority DB;
6. require 690 legacy hotels = 690 NICHE-001 orgs = 690 bridge rows, semantic mismatch count 0, duplicate bridge targets 0, integrity ok, FK 0;
7. run restore/replay and local-first validation;
8. only then transition CP-MN-020 to PASS.

## Local execution note

The ChatGPT execution container attempted to clone the branch for `scripts/local_ci.sh`, but its runtime DNS cannot resolve `github.com`. This is an environment capability blocker, not a software PASS or FAIL. Do not misreport it as local validation. The persisted local harness remains the canonical command for a connected operator environment/Mac mini.

## Safety delta

- canonical hotel writes: 0
- H-ID allocations: 0
- Sheets mutations: 0
- Gmail/outbound actions: 0
- authority promotion: 0

## Next route

`REPAIR H-0580 AUTHORITY DRIFT -> W2 REAL E4 CANARY -> 690/690 SEMANTIC PASS -> CP-MN-020 COMPLETE -> Candidate Truth / Asset W3`
